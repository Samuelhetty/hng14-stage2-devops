# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=change_me_to_a_strong_password

# API
APP_ENV=production

# Frontend — how the frontend container reaches the API container
API_URL=http://api:8000
